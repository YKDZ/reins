{
  "name": "reins",
  "version": "0.1.0",
  "private": true,
  "description": "A deliberately simple harness control plane.",
  "keywords": [
    "agent",
    "cli",
    "codex",
    "harness",
    "qoder"
  ],
  "homepage": "https://github.com/YKDZ/reins#readme",
  "bugs": {
    "url": "https://github.com/YKDZ/reins/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YKDZ/reins.git"
  },
  "bin": {
    "reins": "./dist/reins.js",
    "reins-daemon": "./dist/reins-daemon.js"
  },
  "files": [
    "dist"
  ],
  "os": [
    "linux"
  ],
  "cpu": [
    "x64"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json --pretty false",
    "format:check": "oxfmt --list-different --config ../../oxfmt.config.ts .",
    "format:write": "oxfmt --write --config ../../oxfmt.config.ts .",
    "lint": "oxlint --quiet --format=unix --config ../../oxlint.config.ts --ignore-pattern node_modules .",
    "lint:fix": "oxlint --format=unix --config ../../oxlint.config.ts . --fix",
    "postbuild": "node -e \"const fs = require('node:fs'); fs.copyFileSync('dist/cli.js', 'dist/reins.js'); if (process.platform !== 'win32') fs.chmodSync('dist/reins.js', 0o755)\"",
    "test": "vitest run test/unit --reporter=agent",
    "test:e2e": "vitest run test/e2e --reporter=agent",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@reins/protocol": "workspace:*",
    "@reins/transport": "workspace:*",
    "commander": "catalog:",
    "proper-lockfile": "catalog:",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "@reins/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@types/proper-lockfile": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript-7": "catalog:",
    "vitest": "catalog:"
  },
  "dependenciesMeta": {
    "@reins/protocol": {
      "injected": false
    },
    "@reins/transport": {
      "injected": false
    }
  },
  "engines": {
    "node": ">=24"
  }
}
