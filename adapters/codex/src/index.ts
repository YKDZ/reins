// Codex worker adapter 占位。
import { adapterKitVersion } from "@reins/adapter-kit";
import type { SessionId } from "@reins/protocol";

export const codexMarker = { adapterKitVersion };
export type { SessionId };
