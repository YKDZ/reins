{
  "name": "@reins/codex",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#/*": {
      "source": "./src/*.ts",
      "types": "./src/*.ts",
      "default": "./dist/*.js"
    }
  },
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json --pretty false",
    "codegen:types": "node --conditions=source scripts/generate-app-server-types.ts",
    "format:check": "oxfmt --list-different --config ../../oxfmt.config.ts .",
    "format:write": "oxfmt --write --config ../../oxfmt.config.ts .",
    "lint": "oxlint --quiet --format=unix --config ../../oxlint.config.ts --ignore-pattern node_modules --ignore-pattern src/generated .",
    "lint:fix": "oxlint --format=unix --config ../../oxlint.config.ts --ignore-pattern src/generated . --fix",
    "record:fixture": "node --conditions=source test/scripts/record-fixture.ts",
    "test": "vitest run test",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@reins/adapter-kit": "workspace:*",
    "@reins/protocol": "workspace:*"
  },
  "devDependencies": {
    "@reins/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript-7": "catalog:",
    "vitest": "catalog:"
  },
  "dependenciesMeta": {
    "@reins/adapter-kit": {
      "injected": false
    },
    "@reins/protocol": {
      "injected": false
    }
  },
  "engines": {
    "node": "24"
  }
}
